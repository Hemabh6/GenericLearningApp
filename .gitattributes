# Scripts and container files run on Linux: keep LF line endings even when checked out on Windows,
# or the shell reports "bad interpreter" / "\r: command not found".
*.sh          text eol=lf
Dockerfile    text eol=lf
Caddyfile     text eol=lf
*.yaml        text eol=lf
*.yml         text eol=lf
